### Running the project

Clone this repository :

```
git clone https://github.com/schrish21/PetPals.git
cd pet-pals
```

Install packages :

```
npm install --save --legacy-peer-deps
```

When installation is complete, open Android Studio and run expo:

```
expo start -c

a (to open android emulator)
```
