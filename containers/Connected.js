import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

function ConnectedPage(){
    return(
        <View style={{flex:1, alignItems:'center', justifyContent: 'center'}}>
            <Text> Connected Page </Text>
        </View>
    );
}

export default ConnectedPage;